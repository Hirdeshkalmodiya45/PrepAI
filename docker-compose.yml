services:

  frontend:
    build: ./client
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
  

  # mongodb:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongodb-data:/data/db

# volumes:
#   mongodb-data: